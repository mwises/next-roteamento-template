import styles from '../Help/styles.module.css'
export default function HelpScreen() {
    return(
        <section id='help' className={styles.section}>
            <h1>Help</h1>
        </section>
    );
}